{
  "name": "gamera",
  "private": true,
  "description": "Vite-hosted outbound bridge for game scripting runtimes. The game connects out; each title is a plugin.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "exports": {
    "./vite": "./packages/vite-plugin/src/index.ts"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/hub/tsconfig.json && tsc --noEmit -p packages/client/tsconfig.json && tsc --noEmit -p packages/agent/tsconfig.json && tsc --noEmit -p packages/vite-plugin/tsconfig.json && tsc --noEmit -p packages/sharp-mist/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "22.15.0",
    "@types/ws": "8.18.1",
    "esbuild": "0.25.12",
    "typescript": "5.8.3",
    "vite": "6.3.5",
    "vitest": "3.2.4",
    "ws": "8.18.3"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED",
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
